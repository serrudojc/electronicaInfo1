#include <stdio.h>
#include <stdlib.h>
#include "listmp3lib.h"

// -- Función que lee la información de UN archivo mp3 y la agrega a una lista simplemente enlazada --
int agregar_nodo(nodo_t **L, char *archivo_mp3){

	return 0;	
}

// -- Imprime toda la lista --
void imprimir_lista(nodo_t **L){
}

// -- Imprime UNA estructura de DATOS (mp3data_t)
void imprimir_mp3(mp3data_t *m){
}

// -- Libera toda la lista --
void liberar_lista(nodo_t **L){
}

// -- Libera un nodo de la lista --
void liberar_nodo(nodo_t *n){
}

// -- Ordena la lista por TRACK ASCENDENTE --
void ordenar_lista_x_track(nodo_t **L){
}
